const translations = {
    en: {
        skip: 'Skip to content',
        topMessage: 'Admissions are open for the July 2026 session',
        navHome: 'Home',
        navCourses: 'Courses',
        navAbout: 'About',
        navBranches: 'Branches',
        navServices: 'Student Services',
        navContact: 'Contact',
        getStarted: 'Get Started',
        heroEyebrow: 'Bangladesh National Youth Technical Institute',
        heroTitleOne: 'Practical skills for a',
        heroTitleTwo: 'future without limits.',
        heroBody: 'Industry-focused technical training, experienced mentors, and nationwide opportunity—designed to turn ambition into employable expertise.',
        exploreCourses: 'Explore Courses',
        contactUs: 'Contact Us',
        branchesStat: 'Branches',
        coursesStat: 'Courses',
        learnersStat: 'Learners',
        labTag: 'LEARNING BY DOING',
        labTitle: 'Modern labs. Real-world practice.',
        verified: 'Verified training',
        recognized: 'Industry recognized',
        notice: 'NOTICE',
        noticeText: 'Admission for the July 2026 session is now open • Branch applications are being accepted nationwide • Contact us for course counselling',
        trustOne: 'Govt. aligned',
        trustTwo: 'Practical training',
        trustThree: 'Nationwide network',
        trustFour: 'Career focused',
        programEyebrow: 'CAREER PROGRAMS',
        programTitle: 'Learn the skills employers actually need.',
        programBody: 'Short, practical programs built around real tools, clear outcomes, and skills you can use from day one.',
        courseOne: 'Computer Office Application',
        courseTwo: 'Graphics & Creative Design',
        courseThree: 'Electrical Installation',
        courseFour: 'Web Design & Development',
        courseFive: 'Digital Marketing & SEO',
        courseSix: 'Dress Making & Fashion',
        courseDurationShort: '3–6 Months',
        courseDurationSix: '6 Months',
        courseDurationLong: '6–12 Months',
        courseDescription: 'Hands-on lessons, guided projects, and assessment focused on practical competence.',
        viewCourse: 'View program',
        principalMessage: 'A MESSAGE FROM OUR PRINCIPAL',
        yearsImpact: 'Years of impact',
        aboutEyebrow: 'ABOUT THE INSTITUTE',
        aboutTitle: 'Education that moves beyond the classroom.',
        aboutBodyOne: 'Bangladesh National Youth Technical Institute is committed to developing skilled, confident, and self-reliant people through modern technical education.',
        aboutBodyTwo: 'Our programs combine foundational knowledge with hands-on practice, professional ethics, and the tools learners need to compete in a changing job market.',
        aboutPointOne: 'Experienced instructors',
        aboutPointTwo: 'Practical lab sessions',
        aboutPointThree: 'Flexible course formats',
        aboutPointFour: 'Nationwide access',
        discoverStory: 'Discover our story',
        statStudents: 'Students trained',
        statBranches: 'Active branches',
        statPrograms: 'Career programs',
        statDistricts: 'District reach',
        servicesEyebrow: 'STUDENT SERVICES',
        servicesTitle: 'Everything you need, in one place.',
        servicesBody: 'Clear access to essential academic services—from admission to certification.',
        serviceResults: 'Student Results',
        serviceResultsBody: 'Check published academic and examination results.',
        serviceExam: 'Online Examination',
        serviceExamBody: 'Enter scheduled assessments using verified details.',
        serviceCertificate: 'Certificate Verification',
        serviceCertificateBody: 'Confirm the authenticity of issued credentials.',
        serviceBranch: 'Branch Application',
        serviceBranchBody: 'Apply to join our nationwide training network.',
        serviceAdmission: 'Student Admission',
        serviceAdmissionBody: 'Start a practical learning journey with BNYTI.',
        serviceDownload: 'Download Centre',
        serviceDownloadBody: 'Access forms, notices, cards, and documents.',
        branchesEyebrow: 'NATIONWIDE NETWORK',
        branchesTitle: 'Opportunity, closer to home.',
        branchesBody: 'Our growing network helps learners access consistent technical education across cities, districts, and communities throughout Bangladesh.',
        findBranch: 'Find a branch',
        applyBranch: 'Apply for a branch',
        divisions: 'Divisions',
        districts: 'Districts',
        centres: 'Centres',
        reviewsEyebrow: 'STUDENT STORIES',
        reviewsTitle: 'Confidence earned through practice.',
        reviewsBody: 'Learners share how practical training helped them take the next step.',
        reviewNameOne: 'Tasnim Akter',
        reviewNameTwo: 'Mohammad Al Rafi',
        reviewNameThree: 'Fahim Ahmed',
        reviewCourseOne: 'Computer Office Application',
        reviewCourseTwo: 'Electrical Technology',
        reviewCourseThree: 'Graphics Design',
        reviewOne: 'The classes were clear, supportive, and practical. I can now work confidently with the tools used in a modern office.',
        reviewTwo: 'Learning in the lab made the difference. The instructors connected every lesson to work I may face outside the classroom.',
        reviewThree: 'Project-based learning helped me build both a portfolio and the confidence to present my work to real clients.',
        ctaEyebrow: 'YOUR NEXT STEP STARTS HERE',
        ctaTitle: 'Choose a skill. Create your opportunity.',
        ctaBody: 'Talk with our course counselling team and find the program that fits your goal.',
        callCounsellor: 'Call a counsellor',
        sendEmail: 'Send an email',
        footerAbout: 'Empowering Bangladesh’s next generation through practical technical education and accessible skills development.',
        footerExplore: 'Explore',
        footerSupport: 'Support',
        footerContact: 'Contact',
        studentAdmission: 'Student admission',
        resultCheck: 'Result checking',
        certificateVerify: 'Certificate verification',
        branchApplication: 'Branch Register',
        rights: 'All rights reserved.',
        footerLine: 'Skills for work. Confidence for life.',
    },
    bn: {
        skip: 'মূল কনটেন্টে যান',
        topMessage: 'জুলাই ২০২৬ সেশনে ভর্তি কার্যক্রম চলছে',
        navHome: 'হোম',
        navCourses: 'কোর্সসমূহ',
        navAbout: 'আমাদের সম্পর্কে',
        navBranches: 'শাখাসমূহ',
        navServices: 'শিক্ষার্থী সেবা',
        navContact: 'যোগাযোগ',
        getStarted: 'শুরু করুন',
        heroEyebrow: 'বাংলাদেশ ন্যাশনাল ইয়ুথ টেকনিক্যাল ইনস্টিটিউট',
        heroTitleOne: 'বাস্তবমুখী দক্ষতায়',
        heroTitleTwo: 'সম্ভাবনাময় ভবিষ্যৎ।',
        heroBody: 'শিল্পভিত্তিক কারিগরি প্রশিক্ষণ, অভিজ্ঞ প্রশিক্ষক এবং দেশব্যাপী সুযোগ—আপনার স্বপ্নকে কর্মদক্ষতায় রূপ দেওয়ার জন্য।',
        exploreCourses: 'কোর্স দেখুন',
        contactUs: 'যোগাযোগ করুন',
        branchesStat: 'শাখা',
        coursesStat: 'কোর্স',
        learnersStat: 'শিক্ষার্থী',
        labTag: 'কাজের মাধ্যমে শিক্ষা',
        labTitle: 'আধুনিক ল্যাব। বাস্তব অনুশীলন।',
        verified: 'যাচাইকৃত প্রশিক্ষণ',
        recognized: 'শিল্পখাতে স্বীকৃত',
        notice: 'নোটিশ',
        noticeText: 'জুলাই ২০২৬ সেশনে ভর্তি চলছে • দেশব্যাপী শাখা আবেদন গ্রহণ করা হচ্ছে • কোর্স পরামর্শের জন্য যোগাযোগ করুন',
        trustOne: 'সরকারি নীতিমালা অনুসৃত',
        trustTwo: 'ব্যবহারিক প্রশিক্ষণ',
        trustThree: 'দেশব্যাপী নেটওয়ার্ক',
        trustFour: 'ক্যারিয়ারভিত্তিক',
        programEyebrow: 'ক্যারিয়ার প্রোগ্রাম',
        programTitle: 'চাকরির বাজারে প্রয়োজনীয় দক্ষতা শিখুন।',
        programBody: 'বাস্তব টুলস, সুস্পষ্ট ফলাফল এবং প্রথম দিন থেকেই কাজে লাগানো যায়—এমন সংক্ষিপ্ত ও ব্যবহারিক কোর্স।',
        courseOne: 'কম্পিউটার অফিস অ্যাপ্লিকেশন',
        courseTwo: 'গ্রাফিক্স ও ক্রিয়েটিভ ডিজাইন',
        courseThree: 'ইলেকট্রিক্যাল ইনস্টলেশন',
        courseFour: 'ওয়েব ডিজাইন ও ডেভেলপমেন্ট',
        courseFive: 'ডিজিটাল মার্কেটিং ও এসইও',
        courseSix: 'ড্রেস মেকিং ও ফ্যাশন',
        courseDurationShort: '৩–৬ মাস',
        courseDurationSix: '৬ মাস',
        courseDurationLong: '৬–১২ মাস',
        courseDescription: 'বাস্তব দক্ষতা অর্জনের জন্য হাতে-কলমে পাঠ, গাইডেড প্রজেক্ট এবং মূল্যায়ন।',
        viewCourse: 'প্রোগ্রাম দেখুন',
        principalMessage: 'অধ্যক্ষের বার্তা',
        yearsImpact: 'বছরের অবদান',
        aboutEyebrow: 'ইনস্টিটিউট সম্পর্কে',
        aboutTitle: 'শ্রেণিকক্ষের বাইরেও কার্যকর শিক্ষা।',
        aboutBodyOne: 'বাংলাদেশ ন্যাশনাল ইয়ুথ টেকনিক্যাল ইনস্টিটিউট আধুনিক কারিগরি শিক্ষার মাধ্যমে দক্ষ, আত্মবিশ্বাসী ও স্বনির্ভর মানুষ গড়তে প্রতিশ্রুতিবদ্ধ।',
        aboutBodyTwo: 'আমাদের প্রোগ্রামে মৌলিক জ্ঞান, হাতে-কলমে অনুশীলন, পেশাগত নৈতিকতা এবং পরিবর্তনশীল চাকরির বাজারের প্রয়োজনীয় টুলস একত্রিত করা হয়েছে।',
        aboutPointOne: 'অভিজ্ঞ প্রশিক্ষক',
        aboutPointTwo: 'ব্যবহারিক ল্যাব সেশন',
        aboutPointThree: 'সুবিধাজনক কোর্স পদ্ধতি',
        aboutPointFour: 'দেশব্যাপী সুযোগ',
        discoverStory: 'আমাদের সম্পর্কে জানুন',
        statStudents: 'প্রশিক্ষিত শিক্ষার্থী',
        statBranches: 'সক্রিয় শাখা',
        statPrograms: 'ক্যারিয়ার প্রোগ্রাম',
        statDistricts: 'জেলায় কার্যক্রম',
        servicesEyebrow: 'শিক্ষার্থী সেবা',
        servicesTitle: 'প্রয়োজনীয় সব সেবা, এক জায়গায়।',
        servicesBody: 'ভর্তি থেকে সনদ পর্যন্ত গুরুত্বপূর্ণ একাডেমিক সেবায় সহজ প্রবেশাধিকার।',
        serviceResults: 'শিক্ষার্থী ফলাফল',
        serviceResultsBody: 'প্রকাশিত একাডেমিক ও পরীক্ষার ফলাফল দেখুন।',
        serviceExam: 'অনলাইন পরীক্ষা',
        serviceExamBody: 'যাচাইকৃত তথ্য দিয়ে নির্ধারিত পরীক্ষায় অংশ নিন।',
        serviceCertificate: 'সনদ যাচাই',
        serviceCertificateBody: 'ইস্যুকৃত সনদের সত্যতা নিশ্চিত করুন।',
        serviceBranch: 'শাখা আবেদন',
        serviceBranchBody: 'আমাদের দেশব্যাপী প্রশিক্ষণ নেটওয়ার্কে যুক্ত হতে আবেদন করুন।',
        serviceAdmission: 'শিক্ষার্থী ভর্তি',
        serviceAdmissionBody: 'বিএনওয়াইটিআই-এর সাথে বাস্তবমুখী শিক্ষাযাত্রা শুরু করুন।',
        serviceDownload: 'ডাউনলোড সেন্টার',
        serviceDownloadBody: 'ফরম, নোটিশ, কার্ড ও প্রয়োজনীয় কাগজপত্র সংগ্রহ করুন।',
        branchesEyebrow: 'দেশব্যাপী নেটওয়ার্ক',
        branchesTitle: 'সুযোগ এখন আপনার আরও কাছে।',
        branchesBody: 'বাংলাদেশের শহর, জেলা ও কমিউনিটিতে মানসম্মত কারিগরি শিক্ষা পৌঁছে দিতে আমাদের নেটওয়ার্ক ক্রমাগত বিস্তৃত হচ্ছে।',
        findBranch: 'শাখা খুঁজুন',
        applyBranch: 'শাখার জন্য আবেদন',
        divisions: 'বিভাগ',
        districts: 'জেলা',
        centres: 'কেন্দ্র',
        reviewsEyebrow: 'শিক্ষার্থীদের গল্প',
        reviewsTitle: 'অনুশীলনে অর্জিত আত্মবিশ্বাস।',
        reviewsBody: 'বাস্তবমুখী প্রশিক্ষণ কীভাবে শিক্ষার্থীদের এগিয়ে যেতে সাহায্য করেছে, তাদের মুখেই শুনুন।',
        reviewNameOne: 'তাসনিম আক্তার',
        reviewNameTwo: 'মোহাম্মদ আল রাফি',
        reviewNameThree: 'ফাহিম আহমেদ',
        reviewCourseOne: 'কম্পিউটার অফিস অ্যাপ্লিকেশন',
        reviewCourseTwo: 'ইলেকট্রিক্যাল টেকনোলজি',
        reviewCourseThree: 'গ্রাফিক্স ডিজাইন',
        reviewOne: 'ক্লাসগুলো ছিল সহজবোধ্য, সহায়ক এবং বাস্তবমুখী। এখন আধুনিক অফিসে ব্যবহৃত টুলস নিয়ে আত্মবিশ্বাসের সাথে কাজ করতে পারি।',
        reviewTwo: 'ল্যাবে শেখাই মূল পার্থক্য তৈরি করেছে। প্রশিক্ষকেরা প্রতিটি পাঠকে বাস্তব কাজের সঙ্গে যুক্ত করে বুঝিয়েছেন।',
        reviewThree: 'প্রজেক্টভিত্তিক শিক্ষা আমাকে পোর্টফোলিও তৈরির পাশাপাশি বাস্তব ক্লায়েন্টের সামনে কাজ উপস্থাপনের আত্মবিশ্বাস দিয়েছে।',
        ctaEyebrow: 'আপনার পরবর্তী ধাপ এখানেই',
        ctaTitle: 'একটি দক্ষতা বেছে নিন। নিজের সুযোগ তৈরি করুন।',
        ctaBody: 'আমাদের কোর্স কাউন্সেলিং টিমের সাথে কথা বলুন এবং আপনার লক্ষ্যের উপযুক্ত প্রোগ্রামটি খুঁজে নিন।',
        callCounsellor: 'কাউন্সেলরের সাথে কথা বলুন',
        sendEmail: 'ইমেইল পাঠান',
        footerAbout: 'ব্যবহারিক কারিগরি শিক্ষা ও সহজলভ্য দক্ষতা উন্নয়নের মাধ্যমে বাংলাদেশের আগামী প্রজন্মকে ক্ষমতায়ন করছি।',
        footerExplore: 'এক্সপ্লোর',
        footerSupport: 'সহায়তা',
        footerContact: 'যোগাযোগ',
        studentAdmission: 'শিক্ষার্থী ভর্তি',
        resultCheck: 'ফলাফল দেখুন',
        certificateVerify: 'সনদ যাচাই',
        branchApplication: 'শাখা নিবন্ধন',
        rights: 'সর্বস্বত্ব সংরক্ষিত।',
        footerLine: 'কাজের জন্য দক্ষতা। জীবনের জন্য আত্মবিশ্বাস।',
    },
};

const root = document.documentElement;
const themeToggles = document.querySelectorAll('[data-theme-toggle]');
const localeToggles = document.querySelectorAll('[data-locale-toggle]');
const mobileMenu = document.querySelector('[data-mobile-menu]');
const mobileMenuOverlay = document.querySelector('[data-menu-overlay]');
const mobileMenuDismiss = document.querySelector('[data-menu-dismiss]');
const menuToggle = document.querySelector('[data-menu-toggle]');
const menuOpenIcon = document.querySelector('[data-menu-open]');
const menuCloseIcon = document.querySelector('[data-menu-close]');
const heroCarousel = document.querySelector('[data-hero-carousel]');
const aboutCarousels = document.querySelectorAll('[data-about-carousel]');
const teacherCarousels = document.querySelectorAll('[data-teacher-carousel]');
const studentCarousels = document.querySelectorAll('[data-student-carousel]');
const galleryCarousels = document.querySelectorAll('[data-gallery-carousel]');
const courseCarousels = document.querySelectorAll('[data-course-carousel]');
const newsCarousels = document.querySelectorAll('[data-news-carousel]');

let locale = localStorage.locale === 'bn' ? 'bn' : 'en';
let isMobileMenuOpen = false;

function updateThemeControls() {
    const isDark = root.classList.contains('dark');

    document.querySelectorAll('[data-theme-sun]').forEach((icon) => {
        icon.classList.toggle('hidden', isDark);
    });

    document.querySelectorAll('[data-theme-moon]').forEach((icon) => {
        icon.classList.toggle('hidden', !isDark);
    });

    themeToggles.forEach((button) => {
        button.setAttribute('aria-label', isDark ? 'Use light theme' : 'Use dark theme');
    });
}

function applyLocale(nextLocale) {
    locale = nextLocale;
    localStorage.locale = locale;
    root.lang = locale;
    root.dir = 'ltr';

    document.querySelectorAll('[data-i18n]').forEach((element) => {
        const key = element.dataset.i18n;

        if (translations[locale][key]) {
            element.textContent = translations[locale][key];
        }
    });

    document.querySelectorAll('[data-locale-label]').forEach((label) => {
        label.textContent = locale === 'en' ? 'বাং' : 'EN';
    });

    localeToggles.forEach((button) => {
        button.setAttribute('aria-label', locale === 'en' ? 'বাংলা ভাষা ব্যবহার করুন' : 'Use English language');
    });
}

function setMenuState(isOpen) {
    if (!mobileMenu || !menuToggle) {
        return;
    }

    isMobileMenuOpen = isOpen;
    mobileMenu.classList.toggle('is-open', isOpen);
    mobileMenuOverlay?.classList.toggle('is-open', isOpen);
    mobileMenu.setAttribute('aria-hidden', String(!isOpen));
    mobileMenuOverlay?.setAttribute('aria-hidden', String(!isOpen));
    mobileMenu.inert = !isOpen;
    menuToggle.setAttribute('aria-expanded', String(isOpen));
    menuToggle.setAttribute('aria-label', isOpen ? 'Close menu' : 'Open menu');
    menuOpenIcon?.classList.toggle('hidden', isOpen);
    menuCloseIcon?.classList.toggle('hidden', !isOpen);
    document.body.classList.toggle('overflow-hidden', isOpen);

    if (isOpen) {
        window.requestAnimationFrame(() => mobileMenuDismiss?.focus());
    }
}

themeToggles.forEach((button) => {
    button.addEventListener('click', () => {
        root.classList.toggle('dark');
        localStorage.theme = root.classList.contains('dark') ? 'dark' : 'light';
        updateThemeControls();
    });
});

localeToggles.forEach((button) => {
    button.addEventListener('click', () => {
        applyLocale(locale === 'en' ? 'bn' : 'en');
    });
});

menuToggle?.addEventListener('click', () => {
    setMenuState(!isMobileMenuOpen);
});

mobileMenuOverlay?.addEventListener('click', () => {
    setMenuState(false);
    menuToggle?.focus();
});

mobileMenuDismiss?.addEventListener('click', () => {
    setMenuState(false);
    menuToggle?.focus();
});

mobileMenu?.querySelectorAll('a').forEach((link) => {
    link.addEventListener('click', () => setMenuState(false));
});

mobileMenu?.querySelectorAll('[data-mobile-nav-link]').forEach((link) => {
    link.addEventListener('click', () => {
        mobileMenu.querySelectorAll('[data-mobile-nav-link]').forEach((item) => item.classList.remove('active'));
        link.classList.add('active');
    });
});

document.addEventListener('keydown', (event) => {
    if (event.key === 'Escape') {
        setMenuState(false);
        menuToggle?.focus();
    }

    if (event.key === 'Tab' && isMobileMenuOpen) {
        const focusableElements = [...mobileMenu.querySelectorAll('a[href], button:not([disabled])')];
        const firstFocusable = focusableElements[0];
        const lastFocusable = focusableElements.at(-1);

        if (event.shiftKey && document.activeElement === firstFocusable) {
            event.preventDefault();
            lastFocusable?.focus();
        } else if (!event.shiftKey && document.activeElement === lastFocusable) {
            event.preventDefault();
            firstFocusable?.focus();
        }
    }
});

window.addEventListener('resize', () => {
    if (window.innerWidth >= 1024) {
        setMenuState(false);
    }
});

const revealElements = document.querySelectorAll('.reveal');

document.querySelectorAll('[data-location-form]').forEach((locationForm) => {
    const districtSelect = locationForm.querySelector('[data-district-select]');
    const upazilaSelect = locationForm.querySelector('[data-upazila-select]');
    const postOfficeSelect = locationForm.querySelector('[data-post-office-select]');
    const upazilasByDistrict = JSON.parse(locationForm.dataset.upazilas || '{}');
    const oldUpazila = locationForm.dataset.oldUpazila || '';
    const oldPostOffice = locationForm.dataset.oldPostOffice || '';

    const updatePostOffices = () => {
        if (!postOfficeSelect) {
            return;
        }

        const postOffice = upazilaSelect.value ? `${upazilaSelect.value} Post Office` : '';
        postOfficeSelect.replaceChildren(new Option(postOffice || 'Select upazila first', postOffice));
        postOfficeSelect.disabled = !postOffice;
        if (postOffice === oldPostOffice) {
            postOfficeSelect.value = oldPostOffice;
        }
    };

    const updateUpazilas = () => {
        const upazilas = upazilasByDistrict[districtSelect.value] || [];
        upazilaSelect.replaceChildren(new Option(upazilas.length ? 'Select upazila' : 'No upazilas found', ''));
        upazilas.forEach((upazila) => upazilaSelect.add(new Option(upazila, upazila)));
        upazilaSelect.disabled = upazilas.length === 0;
        if (upazilas.includes(oldUpazila)) {
            upazilaSelect.value = oldUpazila;
        }
        updatePostOffices();
    };

    districtSelect.addEventListener('change', updateUpazilas);
    upazilaSelect.addEventListener('change', updatePostOffices);
    updateUpazilas();
});

document.querySelectorAll('[data-result-form]').forEach((resultForm) => {
    const subjectsBody = resultForm.querySelector('[data-result-subjects]');
    const addButton = resultForm.querySelector('[data-add-result-subject]');
    const semesterSelect = resultForm.querySelector('[data-result-semester]');
    const configuredSubjects = JSON.parse(resultForm.dataset.semesterSubjects || '{}');

    const renumberRows = () => {
        subjectsBody.querySelectorAll('[data-result-subject-row]').forEach((row, index) => {
            row.querySelectorAll('input').forEach((input) => {
                input.name = input.name.replace(/subjects\[\d+\]/, `subjects[${index}]`);
            });
        });
    };

    const renderConfiguredSubjects = (subjects) => {
        if (!subjects.length) {
            return;
        }

        subjectsBody.replaceChildren(...subjects.map((subject, index) => {
            const row = document.createElement('tr');
            row.dataset.resultSubjectRow = '';
            row.innerHTML = `<td class="px-3 py-2"><input name="subjects[${index}][code]" required value="${subject.code}" class="w-full rounded-xl border border-slate-300 px-3 py-2.5 text-sm"></td><td class="px-3 py-2"><input name="subjects[${index}][title]" required value="${subject.title}" class="w-full rounded-xl border border-slate-300 px-3 py-2.5 text-sm"></td><td class="px-3 py-2"><input type="number" name="subjects[${index}][credit]" required min="0.5" max="20" step="0.5" value="${subject.credit}" class="w-full rounded-xl border border-slate-300 px-3 py-2.5 text-sm"></td><td class="px-3 py-2"><input type="number" name="subjects[${index}][marks]" min="0" max="100" step="0.01" class="w-full rounded-xl border border-slate-300 px-3 py-2.5 text-sm"></td><td class="px-3 py-2"><input name="subjects[${index}][grade]" required class="w-full rounded-xl border border-slate-300 px-3 py-2.5 text-sm"></td><td class="px-3 py-2"><input type="number" name="subjects[${index}][grade_point]" required min="0" max="4" step="0.01" class="w-full rounded-xl border border-slate-300 px-3 py-2.5 text-sm"></td><td class="px-3 py-2 text-right"><button type="button" data-remove-result-subject class="font-black text-rose-600">Remove</button></td>`;
            return row;
        }));
    };

    semesterSelect?.addEventListener('change', () => renderConfiguredSubjects(configuredSubjects[semesterSelect.value] || []));

    addButton.addEventListener('click', () => {
        const index = subjectsBody.querySelectorAll('[data-result-subject-row]').length;
        const row = document.createElement('tr');
        row.dataset.resultSubjectRow = '';
        row.innerHTML = `<td class="px-3 py-2"><input name="subjects[${index}][code]" required class="w-full rounded-xl border border-slate-300 px-3 py-2.5 text-sm"></td><td class="px-3 py-2"><input name="subjects[${index}][title]" required class="w-full rounded-xl border border-slate-300 px-3 py-2.5 text-sm"></td><td class="px-3 py-2"><input type="number" name="subjects[${index}][credit]" required min="0.5" max="20" step="0.5" class="w-full rounded-xl border border-slate-300 px-3 py-2.5 text-sm"></td><td class="px-3 py-2"><input type="number" name="subjects[${index}][marks]" min="0" max="100" step="0.01" class="w-full rounded-xl border border-slate-300 px-3 py-2.5 text-sm"></td><td class="px-3 py-2"><input name="subjects[${index}][grade]" required class="w-full rounded-xl border border-slate-300 px-3 py-2.5 text-sm"></td><td class="px-3 py-2"><input type="number" name="subjects[${index}][grade_point]" required min="0" max="4" step="0.01" class="w-full rounded-xl border border-slate-300 px-3 py-2.5 text-sm"></td><td class="px-3 py-2 text-right"><button type="button" data-remove-result-subject class="font-black text-rose-600">Remove</button></td>`;
        subjectsBody.append(row);
    });

    subjectsBody.addEventListener('click', (event) => {
        if (event.target.matches('[data-remove-result-subject]') && subjectsBody.querySelectorAll('[data-result-subject-row]').length > 1) {
            event.target.closest('[data-result-subject-row]').remove();
            renumberRows();
        }
    });
});

if ('IntersectionObserver' in window) {
    const observer = new IntersectionObserver(
        (entries, currentObserver) => {
            entries.forEach((entry) => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('is-visible');
                    currentObserver.unobserve(entry.target);
                }
            });
        },
        { threshold: 0.12 },
    );

    revealElements.forEach((element) => observer.observe(element));
} else {
    revealElements.forEach((element) => element.classList.add('is-visible'));
}

if (heroCarousel) {
    const heroImages = [...heroCarousel.querySelectorAll('[data-hero-image]')];
    const heroDots = [...heroCarousel.querySelectorAll('.hero-carousel-dot')];
    const currentSlideLabel = heroCarousel.querySelector('[data-hero-current]');
    const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    let activeHeroIndex = 0;

    function setActiveHeroImage(nextIndex) {
        activeHeroIndex = nextIndex;

        heroImages.forEach((image, index) => {
            image.classList.toggle('is-active', index === activeHeroIndex);
        });

        heroDots.forEach((dot, index) => {
            dot.classList.toggle('is-active', index === activeHeroIndex);
        });

        if (currentSlideLabel) {
            currentSlideLabel.textContent = String(activeHeroIndex + 1);
        }
    }

    setActiveHeroImage(activeHeroIndex);

    if (!prefersReducedMotion && heroImages.length > 1) {
        window.setInterval(() => {
            setActiveHeroImage((activeHeroIndex + 1) % heroImages.length);
        }, 5200);
    }
}

aboutCarousels.forEach((carousel) => {
    const slides = [...carousel.querySelectorAll('[data-about-slide]')];
    const dots = [...carousel.querySelectorAll('[data-about-dot]')];
    const currentLabel = carousel.querySelector('[data-about-current]');
    const previous = carousel.querySelector('[data-about-prev]');
    const next = carousel.querySelector('[data-about-next]');
    const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    const interval = Number(carousel.dataset.aboutInterval || 10000);
    let activeIndex = 0;
    let timer;
    let paused = false;

    if (slides.length < 2) {
        return;
    }

    const render = (index) => {
        activeIndex = (index + slides.length) % slides.length;
        slides.forEach((slide, slideIndex) => slide.classList.toggle('hidden', slideIndex !== activeIndex));
        dots.forEach((dot, dotIndex) => {
            dot.dataset.active = String(dotIndex === activeIndex);
            dot.setAttribute('aria-selected', String(dotIndex === activeIndex));
        });
        if (currentLabel) {
            currentLabel.textContent = String(activeIndex + 1);
        }
    };

    const stop = () => {
        window.clearInterval(timer);
    };

    const start = () => {
        stop();
        if (!prefersReducedMotion && !paused) {
            timer = window.setInterval(() => render(activeIndex + 1), interval);
        }
    };

    previous?.addEventListener('click', () => { render(activeIndex - 1); start(); });
    next?.addEventListener('click', () => { render(activeIndex + 1); start(); });
    dots.forEach((dot) => dot.addEventListener('click', () => { render(Number(dot.dataset.aboutDot)); start(); }));
    carousel.addEventListener('mouseenter', () => { paused = true; stop(); });
    carousel.addEventListener('mouseleave', () => { paused = false; start(); });
    carousel.addEventListener('focusin', () => { paused = true; stop(); });
    carousel.addEventListener('focusout', (event) => {
        if (!carousel.contains(event.relatedTarget)) {
            paused = false;
            start();
        }
    });

    render(0);
    start();
});

teacherCarousels.forEach((carousel) => {
    const track = carousel.querySelector('[data-teacher-track]');
    const slides = [...carousel.querySelectorAll('[data-teacher-slide]')];
    const controls = carousel.querySelector('.teacher-carousel-controls');
    const previous = carousel.querySelector('[data-teacher-prev]');
    const next = carousel.querySelector('[data-teacher-next]');
    const currentLabel = carousel.querySelector('[data-teacher-current]');
    const totalLabel = carousel.querySelector('[data-teacher-total]');
    const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    const autoplayInterval = Number(carousel.dataset.teacherInterval || 5000);
    let activePage = 0;
    let autoplayTimer;
    let scrollFrame;

    if (!track || slides.length < 2) {
        return;
    }

    const cardsPerPage = () => {
        if (window.innerWidth >= 1024) {
            return 4;
        }

        return window.innerWidth >= 768 ? 2 : 1;
    };

    const totalPages = () => Math.ceil(slides.length / cardsPerPage());

    const render = (nextPage) => {
        const pageCount = totalPages();
        activePage = Math.max(0, Math.min(nextPage, pageCount - 1));
        currentLabel.textContent = String(activePage + 1);
        totalLabel.textContent = String(pageCount);
        previous.disabled = activePage === 0;
        next.disabled = activePage === pageCount - 1;
        controls.classList.toggle('is-unavailable', pageCount < 2);
    };

    const showPage = (nextPage, animate = true) => {
        const pageCount = totalPages();
        const boundedPage = Math.max(0, Math.min(nextPage, pageCount - 1));
        const maximumScroll = track.scrollWidth - track.clientWidth;

        track.scrollTo({
            left: pageCount > 1 ? (boundedPage / (pageCount - 1)) * maximumScroll : 0,
            behavior: animate && !prefersReducedMotion ? 'smooth' : 'auto',
        });
        render(boundedPage);
    };

    const stopAutoplay = () => {
        window.clearInterval(autoplayTimer);
        autoplayTimer = undefined;
    };

    const startAutoplay = () => {
        stopAutoplay();

        if (prefersReducedMotion || totalPages() < 2 || document.hidden || carousel.contains(document.activeElement)) {
            return;
        }

        autoplayTimer = window.setInterval(() => {
            showPage((activePage + 1) % totalPages());
        }, autoplayInterval);
    };

    previous.addEventListener('click', () => showPage(activePage - 1));
    next.addEventListener('click', () => showPage(activePage + 1));

    track.addEventListener('keydown', (event) => {
        if (event.key === 'ArrowLeft' || event.key === 'ArrowRight') {
            event.preventDefault();
            showPage(activePage + (event.key === 'ArrowRight' ? 1 : -1));
        }
    });

    carousel.addEventListener('mouseenter', stopAutoplay);
    carousel.addEventListener('mouseleave', startAutoplay);
    carousel.addEventListener('focusin', stopAutoplay);
    carousel.addEventListener('focusout', (event) => {
        if (!carousel.contains(event.relatedTarget)) {
            startAutoplay();
        }
    });
    carousel.addEventListener('pointerdown', stopAutoplay);
    carousel.addEventListener('pointerup', startAutoplay);
    carousel.addEventListener('pointercancel', startAutoplay);
    window.addEventListener('resize', () => {
        showPage(Math.min(activePage, totalPages() - 1), false);
        startAutoplay();
    });
    document.addEventListener('visibilitychange', () => {
        if (document.hidden) {
            stopAutoplay();
        } else {
            startAutoplay();
        }
    });

    track.addEventListener('scroll', () => {
        window.cancelAnimationFrame(scrollFrame);
        scrollFrame = window.requestAnimationFrame(() => {
            const maximumScroll = track.scrollWidth - track.clientWidth;
            const pageCount = totalPages();
            const closestPage = maximumScroll > 0 ? Math.round((track.scrollLeft / maximumScroll) * (pageCount - 1)) : 0;

            render(closestPage);
        });
    }, { passive: true });

    render(0);
    startAutoplay();
});

studentCarousels.forEach((carousel) => {
    const track = carousel.querySelector('[data-student-track]');
    const slides = [...carousel.querySelectorAll('[data-student-slide]')];
    const controls = carousel.querySelector('.student-carousel-controls');
    const previous = carousel.querySelector('[data-student-prev]');
    const next = carousel.querySelector('[data-student-next]');
    const currentLabel = carousel.querySelector('[data-student-current]');
    const totalLabel = carousel.querySelector('[data-student-total]');
    const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    const autoplayInterval = Number(carousel.dataset.studentInterval || 5000);
    let activePage = 0;
    let autoplayTimer;
    let scrollFrame;

    if (!track || slides.length < 2 || !controls || !previous || !next || !currentLabel || !totalLabel) {
        return;
    }

    const cardsPerPage = () => (window.innerWidth >= 1024 ? 4 : window.innerWidth >= 768 ? 2 : 1);
    const totalPages = () => Math.ceil(slides.length / cardsPerPage());
    const render = (nextPage) => {
        const pageCount = totalPages();
        activePage = Math.max(0, Math.min(nextPage, pageCount - 1));
        currentLabel.textContent = String(activePage + 1);
        totalLabel.textContent = String(pageCount);
        previous.disabled = activePage === 0;
        next.disabled = activePage === pageCount - 1;
        controls.classList.toggle('is-unavailable', pageCount < 2);
    };
    const showPage = (nextPage, animate = true) => {
        const pageCount = totalPages();
        const boundedPage = Math.max(0, Math.min(nextPage, pageCount - 1));
        const maximumScroll = track.scrollWidth - track.clientWidth;
        track.scrollTo({
            left: pageCount > 1 ? (boundedPage / (pageCount - 1)) * maximumScroll : 0,
            behavior: animate && !prefersReducedMotion ? 'smooth' : 'auto',
        });
        render(boundedPage);
    };
    const stopAutoplay = () => {
        window.clearInterval(autoplayTimer);
        autoplayTimer = undefined;
    };
    const startAutoplay = () => {
        stopAutoplay();
        if (prefersReducedMotion || totalPages() < 2 || document.hidden || carousel.contains(document.activeElement)) {
            return;
        }
        autoplayTimer = window.setInterval(() => showPage((activePage + 1) % totalPages()), autoplayInterval);
    };

    previous.addEventListener('click', () => showPage(activePage - 1));
    next.addEventListener('click', () => showPage(activePage + 1));
    track.addEventListener('keydown', (event) => {
        if (event.key === 'ArrowLeft' || event.key === 'ArrowRight') {
            event.preventDefault();
            showPage(activePage + (event.key === 'ArrowRight' ? 1 : -1));
        }
    });
    carousel.addEventListener('mouseenter', stopAutoplay);
    carousel.addEventListener('mouseleave', startAutoplay);
    carousel.addEventListener('focusin', stopAutoplay);
    carousel.addEventListener('focusout', (event) => {
        if (!carousel.contains(event.relatedTarget)) {
            startAutoplay();
        }
    });
    carousel.addEventListener('pointerdown', stopAutoplay);
    carousel.addEventListener('pointerup', startAutoplay);
    carousel.addEventListener('pointercancel', startAutoplay);
    window.addEventListener('resize', () => {
        showPage(Math.min(activePage, totalPages() - 1), false);
        startAutoplay();
    });
    document.addEventListener('visibilitychange', () => (document.hidden ? stopAutoplay() : startAutoplay()));
    track.addEventListener('scroll', () => {
        window.cancelAnimationFrame(scrollFrame);
        scrollFrame = window.requestAnimationFrame(() => {
            const maximumScroll = track.scrollWidth - track.clientWidth;
            const pageCount = totalPages();
            render(maximumScroll > 0 ? Math.round((track.scrollLeft / maximumScroll) * (pageCount - 1)) : 0);
        });
    }, { passive: true });
    render(0);
    startAutoplay();
});

galleryCarousels.forEach((carousel) => {
    const track = carousel.querySelector('[data-gallery-track]');
    const slides = [...carousel.querySelectorAll('[data-gallery-slide]')];
    const controls = carousel.querySelector('.gallery-carousel-controls');
    const previous = carousel.querySelector('[data-gallery-prev]');
    const next = carousel.querySelector('[data-gallery-next]');
    const currentLabel = carousel.querySelector('[data-gallery-current]');
    const totalLabel = carousel.querySelector('[data-gallery-total]');
    const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    const autoplayInterval = Number(carousel.dataset.galleryInterval || 5000);
    let activePage = 0;
    let autoplayTimer;
    let scrollFrame;

    if (!track || slides.length < 2 || !controls || !previous || !next || !currentLabel || !totalLabel) {
        return;
    }

    const cardsPerPage = () => (window.innerWidth >= 1024 ? 4 : window.innerWidth >= 768 ? 2 : 1);
    const totalPages = () => Math.ceil(slides.length / cardsPerPage());
    const render = (nextPage) => {
        const pageCount = totalPages();
        activePage = Math.max(0, Math.min(nextPage, pageCount - 1));
        currentLabel.textContent = String(activePage + 1);
        totalLabel.textContent = String(pageCount);
        previous.disabled = activePage === 0;
        next.disabled = activePage === pageCount - 1;
        controls.classList.toggle('is-unavailable', pageCount < 2);
    };
    const showPage = (nextPage, animate = true) => {
        const pageCount = totalPages();
        const boundedPage = Math.max(0, Math.min(nextPage, pageCount - 1));
        const maximumScroll = track.scrollWidth - track.clientWidth;
        track.scrollTo({
            left: pageCount > 1 ? (boundedPage / (pageCount - 1)) * maximumScroll : 0,
            behavior: animate && !prefersReducedMotion ? 'smooth' : 'auto',
        });
        render(boundedPage);
    };
    const stopAutoplay = () => {
        window.clearInterval(autoplayTimer);
        autoplayTimer = undefined;
    };
    const startAutoplay = () => {
        stopAutoplay();
        if (prefersReducedMotion || totalPages() < 2 || document.hidden || carousel.contains(document.activeElement)) {
            return;
        }
        autoplayTimer = window.setInterval(() => showPage((activePage + 1) % totalPages()), autoplayInterval);
    };

    previous.addEventListener('click', () => showPage(activePage - 1));
    next.addEventListener('click', () => showPage(activePage + 1));
    track.addEventListener('keydown', (event) => {
        if (event.key === 'ArrowLeft' || event.key === 'ArrowRight') {
            event.preventDefault();
            showPage(activePage + (event.key === 'ArrowRight' ? 1 : -1));
        }
    });
    carousel.addEventListener('mouseenter', stopAutoplay);
    carousel.addEventListener('mouseleave', startAutoplay);
    carousel.addEventListener('focusin', stopAutoplay);
    carousel.addEventListener('focusout', (event) => {
        if (!carousel.contains(event.relatedTarget)) {
            startAutoplay();
        }
    });
    carousel.addEventListener('pointerdown', stopAutoplay);
    carousel.addEventListener('pointerup', startAutoplay);
    carousel.addEventListener('pointercancel', startAutoplay);
    window.addEventListener('resize', () => {
        showPage(Math.min(activePage, totalPages() - 1), false);
        startAutoplay();
    });
    document.addEventListener('visibilitychange', () => (document.hidden ? stopAutoplay() : startAutoplay()));
    track.addEventListener('scroll', () => {
        window.cancelAnimationFrame(scrollFrame);
        scrollFrame = window.requestAnimationFrame(() => {
            const maximumScroll = track.scrollWidth - track.clientWidth;
            const pageCount = totalPages();
            render(maximumScroll > 0 ? Math.round((track.scrollLeft / maximumScroll) * (pageCount - 1)) : 0);
        });
    }, { passive: true });
    render(0);
    startAutoplay();
});

courseCarousels.forEach((carousel) => {
    const track = carousel.querySelector('[data-course-track]');
    const slides = [...carousel.querySelectorAll('[data-course-slide]')];
    const controls = carousel.querySelector('.course-carousel-controls');
    const previous = carousel.querySelector('[data-course-prev]');
    const next = carousel.querySelector('[data-course-next]');
    const currentLabel = carousel.querySelector('[data-course-current]');
    const totalLabel = carousel.querySelector('[data-course-total]');
    const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    const autoplayInterval = Number(carousel.dataset.courseInterval || 5000);
    let activePage = 0;
    let autoplayTimer;
    let scrollFrame;

    if (!track || slides.length < 2 || !controls || !previous || !next || !currentLabel || !totalLabel) {
        return;
    }

    const cardsPerPage = () => (window.innerWidth >= 1024 ? 4 : window.innerWidth >= 768 ? 2 : 1);
    const totalPages = () => Math.ceil(slides.length / cardsPerPage());
    const render = (nextPage) => {
        const pageCount = totalPages();
        activePage = Math.max(0, Math.min(nextPage, pageCount - 1));
        currentLabel.textContent = String(activePage + 1);
        totalLabel.textContent = String(pageCount);
        previous.disabled = activePage === 0;
        next.disabled = activePage === pageCount - 1;
        controls.classList.toggle('is-unavailable', pageCount < 2);
    };
    const showPage = (nextPage, animate = true) => {
        const pageCount = totalPages();
        const boundedPage = Math.max(0, Math.min(nextPage, pageCount - 1));
        const maximumScroll = track.scrollWidth - track.clientWidth;
        track.scrollTo({
            left: pageCount > 1 ? (boundedPage / (pageCount - 1)) * maximumScroll : 0,
            behavior: animate && !prefersReducedMotion ? 'smooth' : 'auto',
        });
        render(boundedPage);
    };
    const stopAutoplay = () => {
        window.clearInterval(autoplayTimer);
        autoplayTimer = undefined;
    };
    const startAutoplay = () => {
        stopAutoplay();
        if (prefersReducedMotion || totalPages() < 2 || document.hidden || carousel.contains(document.activeElement)) {
            return;
        }
        autoplayTimer = window.setInterval(() => showPage((activePage + 1) % totalPages()), autoplayInterval);
    };

    previous.addEventListener('click', () => showPage(activePage - 1));
    next.addEventListener('click', () => showPage(activePage + 1));
    track.addEventListener('keydown', (event) => {
        if (event.key === 'ArrowLeft' || event.key === 'ArrowRight') {
            event.preventDefault();
            showPage(activePage + (event.key === 'ArrowRight' ? 1 : -1));
        }
    });
    carousel.addEventListener('mouseenter', stopAutoplay);
    carousel.addEventListener('mouseleave', startAutoplay);
    carousel.addEventListener('focusin', stopAutoplay);
    carousel.addEventListener('focusout', (event) => {
        if (!carousel.contains(event.relatedTarget)) {
            startAutoplay();
        }
    });
    carousel.addEventListener('pointerdown', stopAutoplay);
    carousel.addEventListener('pointerup', startAutoplay);
    carousel.addEventListener('pointercancel', startAutoplay);
    window.addEventListener('resize', () => {
        showPage(Math.min(activePage, totalPages() - 1), false);
        startAutoplay();
    });
    document.addEventListener('visibilitychange', () => (document.hidden ? stopAutoplay() : startAutoplay()));
    track.addEventListener('scroll', () => {
        window.cancelAnimationFrame(scrollFrame);
        scrollFrame = window.requestAnimationFrame(() => {
            const maximumScroll = track.scrollWidth - track.clientWidth;
            const pageCount = totalPages();
            render(maximumScroll > 0 ? Math.round((track.scrollLeft / maximumScroll) * (pageCount - 1)) : 0);
        });
    }, { passive: true });
    render(0);
    startAutoplay();
});

newsCarousels.forEach((carousel) => {
    const track = carousel.querySelector('[data-news-track]');
    const slides = [...carousel.querySelectorAll('[data-news-slide]')];
    const controls = carousel.querySelector('.news-carousel-controls');
    const previous = carousel.querySelector('[data-news-prev]');
    const next = carousel.querySelector('[data-news-next]');
    const currentLabel = carousel.querySelector('[data-news-current]');
    const totalLabel = carousel.querySelector('[data-news-total]');
    const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    const autoplayInterval = Number(carousel.dataset.newsInterval || 5000);
    let activePage = 0;
    let autoplayTimer;

    if (!track || slides.length < 2 || !controls || !previous || !next || !currentLabel || !totalLabel) {
        return;
    }

    const cardsPerPage = () => (window.innerWidth >= 1024 ? 4 : window.innerWidth >= 768 ? 2 : 1);
    const totalPages = () => Math.ceil(slides.length / cardsPerPage());
    const render = (nextPage) => {
        const pageCount = totalPages();
        activePage = Math.max(0, Math.min(nextPage, pageCount - 1));
        currentLabel.textContent = String(activePage + 1);
        totalLabel.textContent = String(pageCount);
        previous.disabled = activePage === 0;
        next.disabled = activePage === pageCount - 1;
        controls.classList.toggle('is-unavailable', pageCount < 2);
    };
    const showPage = (nextPage, animate = true) => {
        const pageCount = totalPages();
        const boundedPage = Math.max(0, Math.min(nextPage, pageCount - 1));
        const maximumScroll = track.scrollWidth - track.clientWidth;
        const targetScroll = pageCount > 1 ? (boundedPage / (pageCount - 1)) * maximumScroll : 0;

        track.style.transitionDuration = animate && !prefersReducedMotion ? '' : '0ms';
        track.style.transform = `translate3d(${-targetScroll}px, 0, 0)`;
        render(boundedPage);
    };
    const stopAutoplay = () => {
        window.clearInterval(autoplayTimer);
        autoplayTimer = undefined;
    };
    const startAutoplay = () => {
        stopAutoplay();
        if (prefersReducedMotion || totalPages() < 2 || document.hidden || carousel.contains(document.activeElement)) {
            return;
        }
        autoplayTimer = window.setInterval(() => showPage((activePage + 1) % totalPages()), autoplayInterval);
    };

    const showPreviousPage = () => showPage(activePage - 1);
    const showNextPage = () => showPage(activePage + 1);

    previous.addEventListener('click', showPreviousPage);
    next.addEventListener('click', showNextPage);
    carousel.addEventListener('news:previous', showPreviousPage);
    carousel.addEventListener('news:next', showNextPage);
    track.addEventListener('keydown', (event) => {
        if (event.key === 'ArrowLeft' || event.key === 'ArrowRight') {
            event.preventDefault();
            showPage(activePage + (event.key === 'ArrowRight' ? 1 : -1));
        }
    });
    carousel.addEventListener('mouseenter', stopAutoplay);
    carousel.addEventListener('mouseleave', startAutoplay);
    carousel.addEventListener('focusin', stopAutoplay);
    carousel.addEventListener('focusout', (event) => {
        if (!carousel.contains(event.relatedTarget)) {
            startAutoplay();
        }
    });
    carousel.addEventListener('pointerdown', stopAutoplay);
    carousel.addEventListener('pointerup', startAutoplay);
    carousel.addEventListener('pointercancel', startAutoplay);
    window.addEventListener('resize', () => {
        showPage(Math.min(activePage, totalPages() - 1), false);
        startAutoplay();
    });
    document.addEventListener('visibilitychange', () => (document.hidden ? stopAutoplay() : startAutoplay()));
    render(0);
    startAutoplay();
});

document.querySelectorAll('[data-print-document]').forEach((button) => {
    button.addEventListener('click', () => window.print());
});

const adminSidebar = document.querySelector('[data-admin-sidebar]');
const adminOverlay = document.querySelector('[data-admin-overlay]');
const adminMenuOpen = document.querySelector('[data-admin-menu-open]');
const adminMenuClose = document.querySelector('[data-admin-menu-close]');

const setAdminMenu = (isOpen) => {
    adminSidebar?.classList.toggle('is-open', isOpen);
    adminOverlay?.classList.toggle('hidden', !isOpen);
    adminMenuOpen?.setAttribute('aria-expanded', String(isOpen));
    document.body.classList.toggle('overflow-hidden', isOpen);
};

adminMenuOpen?.addEventListener('click', () => setAdminMenu(true));
adminMenuClose?.addEventListener('click', () => setAdminMenu(false));
adminOverlay?.addEventListener('click', () => setAdminMenu(false));
document.addEventListener('keydown', (event) => {
    if (event.key === 'Escape') {
        setAdminMenu(false);
    }
});

const confirmDialog = document.querySelector('[data-admin-confirm-dialog]');
const confirmTitle = confirmDialog?.querySelector('[data-confirm-title]');
const confirmMessage = confirmDialog?.querySelector('[data-confirm-message]');
const confirmCancel = confirmDialog?.querySelector('[data-confirm-cancel]');
const confirmProceed = confirmDialog?.querySelector('[data-confirm-proceed]');
let pendingConfirmationForm;

document.querySelectorAll('form[data-confirm], form:has(input[name="_method"][value="DELETE"])').forEach((form) => {
    form.addEventListener('submit', (event) => {
        if (form.dataset.confirmed === 'true') {
            return;
        }

        event.preventDefault();
        pendingConfirmationForm = form;
        confirmTitle.textContent = form.dataset.confirmTitle || 'Please confirm';
        confirmMessage.textContent = form.dataset.confirm || 'Delete this record? This action cannot be undone.';
        confirmDialog.showModal();
        confirmCancel.focus();
    });
});

confirmCancel?.addEventListener('click', () => confirmDialog.close());
confirmProceed?.addEventListener('click', () => {
    if (pendingConfirmationForm) {
        pendingConfirmationForm.dataset.confirmed = 'true';
        confirmDialog.close();
        pendingConfirmationForm.requestSubmit();
    }
});

const editableAdminForms = [...document.querySelectorAll('[data-admin-workspace] form:not([method="GET"]):not([data-no-dirty-check])')]
    .filter((form) => !form.querySelector('input[name="_method"][value="DELETE"]'));
let hasUnsavedAdminChanges = false;

editableAdminForms.forEach((form) => {
    form.addEventListener('input', () => { hasUnsavedAdminChanges = true; });
    form.addEventListener('change', () => { hasUnsavedAdminChanges = true; });
    form.addEventListener('submit', () => { hasUnsavedAdminChanges = false; });
});

window.addEventListener('beforeunload', (event) => {
    if (hasUnsavedAdminChanges) {
        event.preventDefault();
        event.returnValue = '';
    }
});

applyLocale(locale);
updateThemeControls();
