@props(['student' => null, 'result' => null, 'action', 'method' => 'POST', 'submitLabel'])

@php
    $subjects = $result?->subjects?->values() ?? collect([null]);
    $inputClass = 'w-full rounded-xl border border-slate-300 px-3 py-2.5 text-sm outline-none focus:border-blue-600 focus:ring-4 focus:ring-blue-100';
@endphp

<form method="POST" action="{{ $action }}" data-result-form class="space-y-6 rounded-2xl border border-slate-200 bg-white p-6 shadow-lg shadow-slate-900/5 sm:p-8">
    @csrf
    @if ($method !== 'POST') @method($method) @endif
    @if ($student)<input type="hidden" name="student_id" value="{{ $student->id }}">@endif
    <div class="grid gap-5 md:grid-cols-3">
        <label class="grid gap-2 text-sm font-bold text-slate-700">Semester / exam period<input name="semester" required value="{{ old('semester', $result?->semester ?? 'First Year First Semester') }}" class="{{ $inputClass }}">@error('semester')<span class="text-rose-600">{{ $message }}</span>@enderror</label>
        <label class="grid gap-2 text-sm font-bold text-slate-700">Session<input name="session" required value="{{ old('session', $result?->session ?? now()->year) }}" class="{{ $inputClass }}">@error('session')<span class="text-rose-600">{{ $message }}</span>@enderror</label>
        <label class="grid gap-2 text-sm font-bold text-slate-700">Publication status<select name="status" required class="{{ $inputClass }} bg-white"><option value="draft" @selected(old('status', $result?->status ?? 'draft') === 'draft')>Draft</option><option value="published" @selected(old('status', $result?->status) === 'published')>Published</option></select>@error('status')<span class="text-rose-600">{{ $message }}</span>@enderror</label>
    </div>

    <div class="flex items-center justify-between gap-4"><div><h2 class="text-lg font-black text-slate-950">Subject results</h2><p class="text-sm text-slate-500">Add each subject, credit, grade, and grade point.</p></div><button type="button" data-add-result-subject class="rounded-full bg-blue-50 px-4 py-2 text-sm font-black text-blue-700">Add subject</button></div>
    <div class="overflow-x-auto rounded-xl border border-slate-200"><table class="w-full min-w-[900px] text-left text-sm"><thead class="bg-slate-50 text-xs font-black uppercase tracking-wide text-slate-500"><tr><th class="px-3 py-3">Code</th><th class="px-3 py-3">Title</th><th class="px-3 py-3">Credit</th><th class="px-3 py-3">Marks</th><th class="px-3 py-3">Grade</th><th class="px-3 py-3">Grade point</th><th class="px-3 py-3"></th></tr></thead><tbody data-result-subjects>@foreach ($subjects as $index => $subject)<tr data-result-subject-row><td class="px-3 py-2"><input name="subjects[{{ $index }}][code]" required value="{{ old("subjects.$index.code", $subject?->code) }}" class="{{ $inputClass }}">@error("subjects.$index.code")<span class="text-rose-600">{{ $message }}</span>@enderror</td><td class="px-3 py-2"><input name="subjects[{{ $index }}][title]" required value="{{ old("subjects.$index.title", $subject?->title) }}" class="{{ $inputClass }}"></td><td class="px-3 py-2"><input type="number" name="subjects[{{ $index }}][credit]" required min="0.5" max="20" step="0.5" value="{{ old("subjects.$index.credit", $subject?->credit) }}" class="{{ $inputClass }}"></td><td class="px-3 py-2"><input type="number" name="subjects[{{ $index }}][marks]" min="0" max="100" step="0.01" value="{{ old("subjects.$index.marks", $subject?->marks) }}" class="{{ $inputClass }}"></td><td class="px-3 py-2"><input name="subjects[{{ $index }}][grade]" required value="{{ old("subjects.$index.grade", $subject?->grade) }}" class="{{ $inputClass }}"></td><td class="px-3 py-2"><input type="number" name="subjects[{{ $index }}][grade_point]" required min="0" max="4" step="0.01" value="{{ old("subjects.$index.grade_point", $subject?->grade_point) }}" class="{{ $inputClass }}"></td><td class="px-3 py-2 text-right"><button type="button" data-remove-result-subject class="font-black text-rose-600">Remove</button></td></tr>@endforeach</tbody></table></div>
    <div class="flex flex-wrap items-center justify-between gap-4 rounded-xl bg-slate-50 px-4 py-3 text-sm font-black text-slate-700"><span>Totals calculate after saving</span><span>GPA and overall grade are generated automatically.</span></div>
    <div class="flex justify-end gap-3"><a href="{{ $student ? route('super-admin.students.results.index', $student) : route('super-admin.students.index') }}" class="rounded-full border border-slate-300 px-5 py-3 font-black text-slate-700">Cancel</a><button class="rounded-full bg-blue-600 px-5 py-3 font-black text-white">{{ $submitLabel }}</button></div>
</form>
