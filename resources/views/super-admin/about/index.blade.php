<x-dashboard-shell title="About Us Management" eyebrow="Website content" description="Manage the stories that rotate in the public About Us section.">
    @if (session('status'))<div class="mb-6 rounded-xl bg-emerald-50 px-4 py-3 text-sm font-bold text-emerald-700">{{ session('status') }}</div>@endif
    <div class="mb-6 flex justify-end"><a href="{{ route('super-admin.about.create') }}" class="rounded-full bg-blue-600 px-5 py-3 font-black text-white">Add About entry</a></div>
    <div class="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-lg shadow-slate-900/5"><div class="overflow-x-auto"><table class="w-full min-w-[850px] text-left text-sm"><thead class="bg-slate-50 text-xs font-black uppercase text-slate-500"><tr><th class="px-6 py-4">Image</th><th class="px-6 py-4">Heading</th><th class="px-6 py-4">Order</th><th class="px-6 py-4">Status</th><th class="px-6 py-4 text-right">Actions</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse ($abouts as $about)<tr><td class="px-6 py-4">@if($about->image_path)<img src="{{ Storage::disk('public')->url($about->image_path) }}" alt="" class="size-12 rounded-xl object-cover">@else<span class="grid size-12 place-items-center rounded-xl bg-slate-100 text-xs font-black text-slate-400">—</span>@endif</td><td class="px-6 py-4"><p class="font-black">{{ $about->about_heading }}</p><p class="mt-1 max-w-md truncate text-xs text-slate-500">{{ $about->summary }}</p></td><td class="px-6 py-4 font-black">{{ $about->sort_order }}</td><td class="px-6 py-4"><span class="rounded-full px-3 py-1 text-xs font-black {{ $about->is_published ? 'bg-emerald-100 text-emerald-700' : 'bg-slate-100 text-slate-600' }}">{{ $about->is_published ? 'Published' : 'Draft' }}</span></td><td class="px-6 py-4 text-right"><div class="flex justify-end gap-3"><a href="{{ route('super-admin.about.edit', $about) }}" class="font-black text-blue-700">Edit</a><form method="POST" action="{{ route('super-admin.about.publish', $about) }}">@csrf @method('PATCH')<button class="font-black text-emerald-700">{{ $about->is_published ? 'Unpublish' : 'Publish' }}</button></form><form method="POST" action="{{ route('super-admin.about.destroy', $about) }}">@csrf @method('DELETE')<button class="font-black text-rose-700">Delete</button></form></div></td></tr>@empty<tr><td colspan="5" class="px-6 py-12 text-center text-slate-500">No About entries yet.</td></tr>@endforelse</tbody></table></div></div>
    <div class="mt-6">{{ $abouts->links() }}</div>
</x-dashboard-shell>
