<x-dashboard-shell title="News Management" eyebrow="Content management" description="Publish institute announcements and updates.">
    <div class="mb-6 flex justify-end"><a href="{{ route('super-admin.news.create') }}" class="rounded-full bg-emerald-700 px-5 py-3 font-black text-white">Add news</a></div>
    @if(session('status'))<div class="mb-6 rounded-xl bg-emerald-50 px-4 py-3 font-bold text-emerald-800">{{ session('status') }}</div>@endif
    <div class="overflow-hidden rounded-2xl border border-slate-200 bg-white"><table class="w-full text-left"><thead class="bg-slate-50 text-xs font-black uppercase text-slate-500"><tr><th class="px-6 py-4">Title</th><th class="px-6 py-4">Status</th><th class="px-6 py-4">Published</th><th class="px-6 py-4 text-right">Actions</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($news as $item)<tr><td class="px-6 py-4 font-black">{{ $item->title }}</td><td class="px-6 py-4">{{ $item->is_published ? 'Published' : 'Draft' }}</td><td class="px-6 py-4">{{ $item->published_at?->format('d M Y') ?? '—' }}</td><td class="px-6 py-4 text-right"><a href="{{ route('super-admin.news.edit', $item) }}" class="font-black text-emerald-700">Edit</a></td></tr>@empty<tr><td colspan="4" class="px-6 py-10 text-center text-slate-500">No news yet.</td></tr>@endforelse</tbody></table></div><div class="mt-6">{{ $news->links() }}</div>
</x-dashboard-shell>
