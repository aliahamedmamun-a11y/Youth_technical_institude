<x-dashboard-shell title="News Management" eyebrow="Content management" description="Create, publish, and maintain institute announcements.">
    <div class="mb-6 flex flex-wrap items-center justify-between gap-4"><div><p class="text-sm text-slate-500">{{ $news->total() }} total articles</p></div><a href="{{ route('super-admin.news.create') }}" class="inline-flex rounded-full bg-emerald-700 px-5 py-3 font-black text-white">Create news</a></div>
    @if(session('status'))<div class="mb-6 rounded-xl border border-emerald-200 bg-emerald-50 px-4 py-3 font-bold text-emerald-800">{{ session('status') }}</div>@endif
    <div class="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div class="overflow-x-auto"><table class="w-full min-w-[1100px] text-left"><thead class="bg-slate-50 text-xs font-black uppercase tracking-wide text-slate-500"><tr><th class="px-5 py-4">Image</th><th class="px-5 py-4">Title / Slug</th><th class="px-5 py-4">Excerpt</th><th class="px-5 py-4">Content</th><th class="px-5 py-4">Author</th><th class="px-5 py-4">Status</th><th class="px-5 py-4">Published</th><th class="px-5 py-4 text-right">Actions</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($news as $item)<tr class="align-top text-sm"><td class="px-5 py-4">@if($item->image_path)<img src="{{ Storage::disk('public')->url($item->image_path) }}" alt="" class="size-14 rounded-lg object-cover">@else<span class="grid size-14 place-items-center rounded-lg bg-slate-100 text-xs text-slate-400">—</span>@endif</td><td class="px-5 py-4"><p class="font-black text-slate-950">{{ $item->title }}</p><p class="mt-1 text-xs text-slate-400">/{{ $item->slug }}</p></td><td class="max-w-[210px] px-5 py-4 text-slate-600">{{ $item->excerpt ?: '—' }}</td><td class="max-w-[260px] px-5 py-4 text-slate-600">{{ Str::limit($item->content, 100) }}</td><td class="px-5 py-4">{{ $item->author?->name ?? '—' }}</td><td class="px-5 py-4"><span class="rounded-full px-3 py-1 text-xs font-black {{ $item->is_published ? 'bg-emerald-50 text-emerald-700' : 'bg-amber-50 text-amber-700' }}">{{ $item->is_published ? 'Published' : 'Draft' }}</span></td><td class="whitespace-nowrap px-5 py-4">{{ $item->published_at?->format('d M Y, h:i A') ?? '—' }}</td><td class="px-5 py-4 text-right"><div class="flex justify-end gap-3"><a href="{{ route('super-admin.news.edit', $item) }}" class="font-black text-emerald-700">Edit</a><form method="POST" action="{{ route('super-admin.news.destroy', $item) }}" onsubmit="return confirm('Delete this news article?')">@csrf @method('DELETE')<button class="font-black text-rose-600">Delete</button></form></div></td></tr>@empty<tr><td colspan="8" class="px-6 py-12 text-center text-slate-500">No news yet. Create your first announcement.</td></tr>@endforelse</tbody></table></div></div><div class="mt-6">{{ $news->links() }}</div>
</x-dashboard-shell>
