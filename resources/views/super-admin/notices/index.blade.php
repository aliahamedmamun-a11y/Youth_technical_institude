<x-dashboard-shell title="Notice Management" eyebrow="Content management" description="Create and publish short homepage announcements separately from News articles.">
    <div class="mb-6 flex flex-wrap items-center justify-between gap-4"><p class="text-sm text-slate-500">{{ $notices->total() }} total notices</p><a href="{{ route('super-admin.notices.create') }}" class="inline-flex rounded-full bg-emerald-700 px-5 py-3 font-black text-white">Create notice</a></div>
    @if(session('status'))<div class="mb-6 rounded-xl border border-emerald-200 bg-emerald-50 px-4 py-3 font-bold text-emerald-800">{{ session('status') }}</div>@endif
    <div class="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div class="overflow-x-auto"><table class="w-full min-w-[850px] text-left"><thead class="bg-slate-50 text-xs font-black uppercase tracking-wide text-slate-500"><tr><th class="px-5 py-4">Title</th><th class="px-5 py-4">Message</th><th class="px-5 py-4">Author</th><th class="px-5 py-4">Status</th><th class="px-5 py-4">Published</th><th class="px-5 py-4 text-right">Actions</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($notices as $notice)<tr class="align-top text-sm"><td class="px-5 py-4 font-black text-slate-950">{{ $notice->title }}</td><td class="max-w-[320px] px-5 py-4 text-slate-600">{{ $notice->message }}</td><td class="px-5 py-4">{{ $notice->author?->name ?? '—' }}</td><td class="px-5 py-4"><span class="rounded-full px-3 py-1 text-xs font-black {{ $notice->is_published ? 'bg-emerald-50 text-emerald-700' : 'bg-amber-50 text-amber-700' }}">{{ $notice->is_published ? 'Published' : 'Draft' }}</span></td><td class="whitespace-nowrap px-5 py-4">{{ $notice->published_at?->format('d M Y, h:i A') ?? '—' }}</td><td class="px-5 py-4 text-right"><div class="flex justify-end gap-3"><a href="{{ route('super-admin.notices.edit', $notice) }}" class="font-black text-emerald-700">Edit</a><form method="POST" action="{{ route('super-admin.notices.destroy', $notice) }}" onsubmit="return confirm('Delete this notice?')">@csrf @method('DELETE')<button class="font-black text-rose-600">Delete</button></form></div></td></tr>@empty<tr><td colspan="6" class="px-6 py-12 text-center text-slate-500">No notices yet. Create your first notice.</td></tr>@endforelse</tbody></table></div></div><div class="mt-6">{{ $notices->links() }}</div>
</x-dashboard-shell>
