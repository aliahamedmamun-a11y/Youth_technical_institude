<x-dashboard-shell :title="$course->name.' Semesters'" eyebrow="Academic setup" description="Manage the reusable semester and subject structure for this course.">
    <div class="mb-6 flex flex-wrap justify-between gap-3"><a href="{{ route('super-admin.courses.index') }}" class="rounded-full border border-slate-300 px-5 py-3 text-sm font-black text-slate-700">Back to courses</a><a href="{{ route('super-admin.courses.semesters.create', $course) }}" class="rounded-full bg-blue-600 px-5 py-3 text-sm font-black text-white">Add semester</a></div>
    <div class="grid gap-4">@forelse ($semesters as $semester)<article class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div class="flex flex-wrap justify-between gap-3"><div><h2 class="text-lg font-black">{{ $semester->name }}</h2><p class="mt-1 text-sm text-slate-500">{{ $semester->subjects_count }} subjects · Order {{ $semester->sort_order }}</p></div><span class="rounded-full px-3 py-1 text-xs font-black {{ $semester->is_active ? 'bg-emerald-50 text-emerald-700' : 'bg-slate-100 text-slate-500' }}">{{ $semester->is_active ? 'Active' : 'Inactive' }}</span></div><div class="mt-5 flex flex-wrap gap-3"><a href="{{ route('super-admin.semesters.subjects.index', $semester) }}" class="rounded-full bg-slate-950 px-4 py-2 text-sm font-black text-white">Manage subjects</a><a href="{{ route('super-admin.courses.semesters.edit', [$course, $semester]) }}" class="rounded-full border border-slate-300 px-4 py-2 text-sm font-black text-slate-700">Edit</a><form method="POST" action="{{ route('super-admin.courses.semesters.destroy', [$course, $semester]) }}">@csrf @method('DELETE')<button class="rounded-full border border-rose-300 px-4 py-2 text-sm font-black text-rose-700">Delete</button></form></div></article>@empty<div class="rounded-2xl border border-dashed border-slate-300 bg-white p-12 text-center text-slate-500">No semesters configured for this course.</div>@endforelse</div>
</x-dashboard-shell>
