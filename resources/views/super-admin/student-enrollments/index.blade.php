<x-dashboard-shell :title="$student->name.' Semester Assignments'" eyebrow="Academic records" :description="$student->course->name.' · Assign semesters and subjects before entering marks.'">
    <div class="mb-6 flex flex-wrap justify-between gap-3"><a href="{{ route('super-admin.students.show', $student) }}" class="rounded-full border border-slate-300 px-5 py-3 text-sm font-black text-slate-700">Back to student</a><a href="{{ route('super-admin.students.semester-enrollments.create', $student) }}" class="rounded-full bg-emerald-700 px-5 py-3 text-sm font-black text-white">Assign semester</a></div>
    <div class="grid gap-4">@forelse ($enrollments as $enrollment)<article class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div class="flex flex-wrap justify-between gap-3"><div><h2 class="text-lg font-black">{{ $enrollment->semester->name }}</h2><p class="mt-1 text-sm text-slate-500">{{ $enrollment->subjects->count() }} assigned subjects · {{ ucfirst($enrollment->status) }}</p></div><span class="rounded-full bg-emerald-50 px-3 py-1 text-xs font-black text-emerald-700">Assigned</span></div><div class="mt-5 flex flex-wrap gap-3">@if ($enrollment->results->isNotEmpty())<a href="{{ route('super-admin.results.edit', $enrollment->results->first()) }}" class="rounded-full bg-blue-600 px-4 py-2 text-sm font-black text-white">Edit marks</a>@else<a href="{{ route('super-admin.enrollments.results.create', $enrollment) }}" class="rounded-full bg-blue-600 px-4 py-2 text-sm font-black text-white">Enter marks</a>@endif<a href="{{ route('super-admin.students.semester-enrollments.edit', [$student, $enrollment]) }}" class="rounded-full border border-slate-300 px-4 py-2 text-sm font-black text-slate-700">Manage subjects</a><form method="POST" action="{{ route('super-admin.students.semester-enrollments.destroy', [$student, $enrollment]) }}">@csrf @method('DELETE')<button class="rounded-full border border-rose-300 px-4 py-2 text-sm font-black text-rose-700">Remove</button></form></div><div class="mt-5 grid gap-2 text-sm text-slate-600 sm:grid-cols-2">@foreach ($enrollment->subjects as $subject)<div class="rounded-lg bg-slate-50 px-3 py-2"><strong>{{ $subject->code }}</strong> · {{ $subject->title }} · {{ $subject->credit }} credit</div>@endforeach</div></article>@empty<div class="rounded-2xl border border-dashed border-slate-300 bg-white p-12 text-center text-slate-500">No semester has been assigned to this student.</div>@endforelse</div>
</x-dashboard-shell>
