<x-dashboard-shell :title="$student->name.' Results'" eyebrow="Result management" :description="$student->course->name.' · '.($student->registration_number ?? 'Registration pending')">
    <div class="mb-6 flex flex-wrap justify-between gap-3"><a href="{{ route('super-admin.students.show', $student) }}" class="rounded-full border border-slate-300 px-5 py-3 text-sm font-black text-slate-700">Back to student</a><a href="{{ route('super-admin.students.results.create', $student) }}" class="rounded-full bg-blue-600 px-5 py-3 text-sm font-black text-white">Add result</a></div>
    <div class="grid gap-4">@forelse ($results as $result)<article class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div class="flex flex-wrap items-start justify-between gap-4"><div><p class="text-lg font-black text-slate-950">{{ $result->semester }}</p><p class="mt-1 text-sm text-slate-500">Session {{ $result->session }} · {{ $result->subjects->count() }} subjects</p></div><span class="rounded-full px-3 py-1 text-xs font-black {{ $result->isPublished() ? 'bg-emerald-50 text-emerald-700' : 'bg-amber-50 text-amber-700' }}">{{ ucfirst($result->status) }}</span></div><div class="mt-5 grid gap-4 sm:grid-cols-4"><div><p class="text-xs font-black uppercase text-slate-400">Total credit</p><p class="mt-1 font-black">{{ $result->total_credit }}</p></div><div><p class="text-xs font-black uppercase text-slate-400">Credit earned</p><p class="mt-1 font-black">{{ $result->credit_earned }}</p></div><div><p class="text-xs font-black uppercase text-slate-400">GPA</p><p class="mt-1 font-black">{{ $result->gpa ?? '—' }}</p></div><div><p class="text-xs font-black uppercase text-slate-400">Grade</p><p class="mt-1 font-black">{{ $result->overall_grade ?? '—' }}</p></div></div><div class="mt-5 flex flex-wrap gap-3"><a href="{{ route('super-admin.results.show', $result) }}" class="rounded-full bg-slate-950 px-4 py-2 text-sm font-black text-white">Preview / print</a><a href="{{ route('super-admin.results.edit', $result) }}" class="rounded-full border border-slate-300 px-4 py-2 text-sm font-black text-slate-700">Edit</a><form method="POST" action="{{ route('super-admin.results.destroy', $result) }}">@csrf @method('DELETE')<button class="rounded-full border border-rose-300 px-4 py-2 text-sm font-black text-rose-700">Delete</button></form></div></article>@empty<div class="rounded-2xl border border-dashed border-slate-300 bg-white p-12 text-center text-slate-500">No results have been created for this student.</div>@endforelse</div>
</x-dashboard-shell>
