<x-dashboard-shell title="Student Management" eyebrow="Academic management" description="Register students, maintain records, and prepare official academic documents.">
    <div class="mb-6 flex justify-end"><a href="{{ route('super-admin.students.create') }}" class="rounded-full bg-emerald-700 px-5 py-3 text-sm font-black text-white">Add student</a></div>
    @if(session('status'))<div class="mb-6 rounded-xl border border-emerald-200 bg-emerald-50 px-4 py-3 font-bold text-emerald-800">{{ session('status') }}</div>@endif
    <div class="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-lg shadow-slate-900/5"><div class="overflow-x-auto"><table class="w-full min-w-[820px] text-left"><thead class="bg-slate-50 text-xs font-black uppercase tracking-[.14em] text-slate-500"><tr><th class="px-6 py-4">Photo</th><th class="px-6 py-4">Student</th><th class="px-6 py-4">Registration</th><th class="px-6 py-4">Course</th><th class="px-6 py-4">Result</th><th class="px-6 py-4 text-right">Actions</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($students as $student)<tr class="text-sm"><td class="px-6 py-4">@if($student->image_path)<img src="{{ Storage::disk('public')->url($student->image_path) }}" alt="{{ $student->name }}" class="size-11 rounded-full object-cover">@else<span class="grid size-11 place-items-center rounded-full bg-slate-100 text-xs font-black text-slate-400">—</span>@endif</td><td class="px-6 py-4 font-black text-slate-950">{{ $student->name }}</td><td class="px-6 py-4">{{ $student->registration_number }}</td><td class="px-6 py-4">{{ $student->course->name }}</td><td class="px-6 py-4">{{ $student->result_status }}</td><td class="px-6 py-4 text-right"><a href="{{ route('super-admin.students.show', $student) }}" class="font-black text-emerald-700">Manage</a></td></tr>@empty<tr><td colspan="6" class="px-6 py-12 text-center text-slate-500">No students have been added yet.</td></tr>@endforelse</tbody></table></div></div><div class="mt-6">{{ $students->links() }}</div>
</x-dashboard-shell>
