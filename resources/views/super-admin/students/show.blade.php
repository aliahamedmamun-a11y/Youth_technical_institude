<x-dashboard-shell :title="$student->name" eyebrow="Student profile" :description="$student->registration_number.' · '.$student->course->name">
    @if(session('status'))<div class="mb-6 rounded-xl bg-emerald-50 px-4 py-3 font-bold text-emerald-800">{{ session('status') }}</div>@endif
    <div class="grid gap-6 lg:grid-cols-3"><article class="rounded-2xl border border-slate-200 bg-white p-6 lg:col-span-2"><dl class="grid gap-6 sm:grid-cols-2"><div><dt class="text-xs font-black uppercase text-slate-500">Course</dt><dd class="mt-2 font-black">{{ $student->course->name }}</dd></div><div><dt class="text-xs font-black uppercase text-slate-500">Phone</dt><dd class="mt-2 font-black">{{ $student->phone }}</dd></div><div><dt class="text-xs font-black uppercase text-slate-500">Admission date</dt><dd class="mt-2 font-black">{{ $student->admitted_at->format('d M Y') }}</dd></div><div><dt class="text-xs font-black uppercase text-slate-500">Result</dt><dd class="mt-2 font-black">{{ $student->result_status }} {{ $student->grade ? '· '.$student->grade : '' }}</dd></div></dl><div class="mt-8 flex gap-3"><a href="{{ route('super-admin.students.edit', $student) }}" class="rounded-full bg-emerald-700 px-5 py-3 font-black text-white">Edit student</a><form method="POST" action="{{ route('super-admin.students.destroy', $student) }}">@csrf @method('DELETE')<button class="rounded-full border border-rose-300 px-5 py-3 font-black text-rose-700">Delete</button></form></div></article><aside class="rounded-2xl border border-slate-200 bg-white p-6"><p class="text-xs font-black uppercase tracking-[.14em] text-emerald-700">Documents</p><div class="mt-4 grid gap-2">@foreach(['admit-card' => 'Admit Card', 'registration-card' => 'Registration Card', 'student-id' => 'Student ID', 'certificate' => 'Certificate', 'testimonial' => 'Testimonial', 'transcript' => 'Transcript', 'forwarding-letter' => 'Forwarding Letter', 'results' => 'Results'] as $document => $label)<a href="{{ route('super-admin.students.documents.show', [$student, $document]) }}" class="rounded-xl bg-slate-50 px-4 py-3 text-sm font-black text-slate-700 hover:bg-emerald-50 hover:text-emerald-800">{{ $label }}</a>@endforeach</div></aside></div>
</x-dashboard-shell>
