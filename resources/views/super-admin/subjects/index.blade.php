<x-dashboard-shell :title="$semester->name.' Subjects'" eyebrow="Academic setup" :description="$semester->course->name.' · Manage code, title, credit, and order.'"><div class="mb-6 flex flex-wrap justify-between gap-3"><a href="{{ route('super-admin.courses.semesters.index', $semester->course) }}" class="rounded-full border border-slate-300 px-5 py-3 text-sm font-black text-slate-700">Back to semesters</a><a href="{{ route('super-admin.semesters.subjects.create', $semester) }}" class="rounded-full bg-blue-600 px-5 py-3 text-sm font-black text-white">Add subject</a></div><div class="overflow-x-auto rounded-2xl border border-slate-200 bg-white shadow-sm"><table class="w-full min-w-[700px] text-left text-sm"><thead class="bg-slate-50 text-xs font-black uppercase text-slate-500"><tr><th class="px-5 py-4">Code</th><th class="px-5 py-4">Title</th><th class="px-5 py-4">Credit</th><th class="px-5 py-4">Order</th><th class="px-5 py-4">Status</th><th class="px-5 py-4"></th></tr></thead><tbody>@forelse ($subjects as $subject)<tr><td class="border-t px-5 py-4 font-black">{{ $subject->code }}</td><td class="border-t px-5 py-4">{{ $subject->title }}</td><td class="border-t px-5 py-4">{{ $subject->credit }}</td><td class="border-t px-5 py-4">{{ $subject->sort_order }}</td><td class="border-t px-5 py-4">{{ $subject->is_active ? 'Active' : 'Inactive' }}</td><td class="border-t px-5 py-4 text-right"><a href="{{ route('super-admin.semesters.subjects.edit', [$semester, $subject]) }}" class="font-black text-blue-700">Edit</a><form class="mt-2" method="POST" action="{{ route('super-admin.semesters.subjects.destroy', [$semester, $subject]) }}">@csrf @method('DELETE')<button class="font-black text-rose-700">Delete</button></form></td></tr>@empty<tr><td colspan="6" class="px-5 py-12 text-center text-slate-500">No subjects configured.</td></tr>@endforelse</tbody></table></div></x-dashboard-shell>
