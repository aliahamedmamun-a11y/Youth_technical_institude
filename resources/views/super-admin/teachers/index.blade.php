<x-dashboard-shell title="Teacher Management" eyebrow="Academic management" description="Maintain teaching staff records and responsibilities.">
    <div class="mb-6 flex justify-end"><a href="{{ route('super-admin.teachers.create') }}" class="rounded-full bg-emerald-700 px-5 py-3 text-sm font-black text-white">Add teacher</a></div>
    @if(session('status'))<div class="mb-6 rounded-xl bg-emerald-50 px-4 py-3 font-bold text-emerald-800">{{ session('status') }}</div>@endif
    <div class="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-lg shadow-slate-900/5"><div class="overflow-x-auto"><table class="w-full min-w-[780px] text-left"><thead class="bg-slate-50 text-xs font-black uppercase text-slate-500"><tr><th class="px-6 py-4">Photo</th><th class="px-6 py-4">Teacher</th><th class="px-6 py-4">Employee No.</th><th class="px-6 py-4">Designation</th><th class="px-6 py-4">Department</th><th class="px-6 py-4 text-right">Actions</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($teachers as $teacher)<tr><td class="px-6 py-4">@if($teacher->image_path)<img src="{{ Storage::disk('public')->url($teacher->image_path) }}" alt="{{ $teacher->name }}" class="size-11 rounded-full object-cover">@else<span class="grid size-11 place-items-center rounded-full bg-slate-100 text-xs font-black text-slate-400">—</span>@endif</td><td class="px-6 py-4 font-black">{{ $teacher->name }}</td><td class="px-6 py-4">{{ $teacher->employee_number }}</td><td class="px-6 py-4">{{ $teacher->designation }}</td><td class="px-6 py-4">{{ $teacher->department ?? '—' }}</td><td class="px-6 py-4 text-right"><a href="{{ route('super-admin.teachers.show', $teacher) }}" class="font-black text-emerald-700">Manage</a></td></tr>@empty<tr><td colspan="6" class="px-6 py-12 text-center text-slate-500">No teachers have been added yet.</td></tr>@endforelse</tbody></table></div></div><div class="mt-6">{{ $teachers->links() }}</div>
</x-dashboard-shell>
